class Actor {
  constructor(startingPosition, startingVelocity, startingRotation, size) {
    this.position = startingPosition;
    this.velocity = startingVelocity;
    this.rotation = startingRotation;
    this.size = size;
  }
}
